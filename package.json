{
  "name": "wolf-night-party",
  "version": "0.1.0",
  "private": true,
  "description": "Wolf Night Party monorepo workspace",
  "packageManager": "pnpm@10.24.0",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
